# Copy to .env (project root). The web server and CLI both read it.
# Without a key, /api/ask still works — it falls back to deterministic
# template prose instead of Claude-written rationale + clarifying questions.
ANTHROPIC_API_KEY=
